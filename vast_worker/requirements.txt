# EXACT stack from the proven RunPod handler (serverless_worker_unified), which
# generated all 62 images cleanly with FLUX 8-bit. torch is installed in the
# Dockerfile (cu121). The "index 21 out of bounds" on Vast was traced to
# accelerate==0.34.2 (pinned old): enable_model_cpu_offload() lives in accelerate,
# and 0.34.2 mishandles 8-bit device placement. RunPod leaves accelerate UNPINNED
# (latest), so we do the same. Let transformers 4.45.2 pull its own tokenizers/hub.
diffusers==0.31.0
transformers==4.45.2
accelerate
safetensors
sentencepiece
protobuf
bitsandbytes>=0.43.0
hf_transfer>=0.1.6
fastapi>=0.111
uvicorn>=0.29
Pillow>=10
